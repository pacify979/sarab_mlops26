# Lean dependency set for the SERVING container only (api/).
# Excludes monitoring/EDA libraries (evidently, plotly, statsmodels, nltk...) that
# the inference service does not need, keeping the Docker image small.
#
# scikit-learn is pinned to the exact version used for training so the pickled
# model (models/model.joblib) unpickles without version-mismatch warnings.
pandas>=2.0,<3.0
numpy>=1.24,<2.0
scikit-learn==1.5.2
joblib>=1.3
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29
pydantic>=2.6
